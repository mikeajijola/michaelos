# Profile image

Add Mike's profile photograph here as:

`michael_portrait.svg`

The same square SVG is used for the centred homepage portrait and the circular
avatar beside the MikeOS wordmark. The interface crops it to a circle with
`object-position: center` behaviour.

Until the photograph is added, both locations show the `MA` fallback.
