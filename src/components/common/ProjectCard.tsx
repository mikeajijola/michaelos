"use client";
import type { Project } from "@/data/content";
import { useCapabilities } from "@/capabilities/context";
import { CapabilityInfo } from "./CapabilityInfo";
export function ProjectCard({project}:{project:Project}){const {execute,selectElement}=useCapabilities();const params={slug:project.slug};const label=`Open ${project.name} project details`;return <div className="project-card-wrap"><button className="project-card" data-capability-id="project.view" data-capability-params={JSON.stringify(params)} aria-label={label} onFocus={()=>selectElement({text:`View ${project.name} project`,role:"button",accessibleName:label,capabilityId:"project.view",params,focused:true})} onMouseEnter={()=>selectElement({text:`View ${project.name} project`,role:"button",accessibleName:label,capabilityId:"project.view",params})} onClick={()=>execute("project.view",params,"ui")}><span className="project-top"><span className="orb"/>{project.year}</span><h3>{project.name}</h3><p>{project.summary}</p><div className="tags">{project.technologies.slice(0,3).map(t=><span className="tag" key={t}>{t}</span>)}<span className="arrow">↗</span></div></button><CapabilityInfo capabilityId="project.view" params={params} controlLabel={label}/></div>}
