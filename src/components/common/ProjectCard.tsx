"use client";
import type { Project } from "@/data/content"; import { useCapabilities } from "@/capabilities/context";
export function ProjectCard({project}:{project:Project}){const {execute,selectElement}=useCapabilities();const params={slug:project.slug};return <button className="project-card" aria-label={`Open ${project.name} project details`} onFocus={()=>selectElement({text:`View ${project.name} project`,role:"button",accessibleName:`Open ${project.name} project details`,capabilityId:"project.view",params})} onMouseEnter={()=>selectElement({text:`View ${project.name} project`,role:"button",accessibleName:`Open ${project.name} project details`,capabilityId:"project.view",params})} onClick={()=>execute("project.view",params,"ui")}><span className="project-top"><span className="orb"/>{project.year}</span><h3>{project.name}</h3><p>{project.summary}</p><div className="tags">{project.technologies.slice(0,3).map(t=><span className="tag" key={t}>{t}</span>)}<span className="arrow">↗</span></div></button>}
