"use client";
import { usePathname } from "next/navigation";
import { useCapabilities } from "@/capabilities/context";
const nav = [["Projects","navigation.goProjects"],["Experience","navigation.goExperience"],["Writing","navigation.goBlog"],["CV","navigation.goCv"]] as const;
export function SiteShell({ children }: { children: React.ReactNode }) { const { execute } = useCapabilities(); const pathname=usePathname(); return <><header className="shell nav"><button className="brand" onClick={()=>execute("navigation.goHome")} aria-label="Go to homepage"><span className="brand-mark">MA</span>Mike Ajijola</button><nav className="links" aria-label="Primary navigation">{nav.map(([label,id])=><button className={pathname.toLowerCase().includes(label.toLowerCase())?"active":""} key={id} onClick={()=>execute(id)}>{label}</button>)}<button className="nav-cta" onClick={()=>execute("navigation.goCapabilities")}>Explore capabilities</button></nav></header><main>{children}</main><Footer/></> }
function Footer(){const {execute}=useCapabilities();return <footer className="footer"><div className="shell"><div className="footer-grid"><div><h2>Let’s make complex things<br/>feel considered.</h2><p>I work with teams building platforms, infrastructure products and ambitious software systems.</p></div><div><b>Explore</b><button onClick={()=>execute("navigation.goProjects")}>Projects</button><button onClick={()=>execute("navigation.goExperience")}>Experience</button><button onClick={()=>execute("navigation.goBlog")}>Writing</button></div><div><b>System</b><button onClick={()=>execute("navigation.goCapabilities")}>Capability registry</button><button onClick={()=>execute("cv.exportJson")}>Export CV data</button></div></div><div className="footer-bottom"><span>© 2026 Mike Ajijola</span><span>Browser-native · local-first · capability-driven</span></div></div></footer>}
