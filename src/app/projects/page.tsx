"use client"; import { useState } from "react"; import { projects } from "@/data/content"; import { ProjectCard } from "@/components/common/ProjectCard";
export default function Projects(){const [q,setQ]=useState("");const shown=projects.filter(p=>[p.name,p.summary,...p.technologies].join(" ").toLowerCase().includes(q.toLowerCase()));return <div className="shell"><div className="page-head"><div className="eyebrow">Selected work</div><h1 className="page-title">Systems made useful.</h1><p>Platforms, products and public-interest technology designed to make difficult work feel more dependable.</p><div className="toolbar"><input className="search" value={q} onChange={e=>setQ(e.target.value)} placeholder="Filter projects…" aria-label="Filter projects"/></div></div><div className="grid3" style={{paddingBottom:90}}>{shown.map(p=><ProjectCard project={p} key={p.id}/>)}</div></div>}
