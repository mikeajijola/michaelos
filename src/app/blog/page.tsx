"use client"; import { articles } from "@/data/content"; import { useCapabilities } from "@/capabilities/context";
export default function Blog(){const {execute}=useCapabilities();return <div className="shell"><div className="page-head"><div className="eyebrow">Field notes</div><h1 className="page-title">Thinking in public.</h1><p>Essays about platforms, local-first software, technical leadership and making systems easier to understand.</p></div><div className="articles" style={{paddingBottom:100}}>{articles.map(a=><button className="article" key={a.id} onClick={()=>execute("article.view",{slug:a.slug})}><div className="meta"><span>{a.publishedAt}</span><span>{a.readTime}</span></div><h3>{a.title}</h3><p>{a.summary}</p><div className="tags">{a.tags.map(t=><span className="tag" key={t}>{t}</span>)}</div></button>)}</div></div>}
