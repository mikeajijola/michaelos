"use client";
import { articles } from "@/data/content";
import { CapabilityButton } from "@/components/common/CapabilityInfo";
export default function Blog(){return <div className="shell"><div className="page-head"><div className="eyebrow">Field notes</div><h1 className="page-title">Thinking in public.</h1><p>Essays about platforms, local-first software, technical leadership and making systems easier to understand.</p></div><div className="articles" style={{paddingBottom:100}}>{articles.map(a=><CapabilityButton className="article-control" buttonClassName="article" capabilityId="article.view" params={{slug:a.slug}} label={`Open ${a.title}`} key={a.id}><><div className="meta"><span>{a.publishedAt}</span><span>{a.readTime}</span></div><h3>{a.title}</h3><p>{a.summary}</p><div className="tags">{a.tags.map(t=><span className="tag" key={t}>{t}</span>)}</div></></CapabilityButton>)}</div></div>}
