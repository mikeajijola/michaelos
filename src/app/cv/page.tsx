"use client"; import { experience,projects,skills } from "@/data/content"; import { useCapabilities } from "@/capabilities/context";
export default function CV(){const {execute}=useCapabilities();const exportJson=async()=>{const e=await execute("cv.exportJson");if(e.success){const a=document.createElement("a");a.href=URL.createObjectURL(new Blob([JSON.stringify(e.result,null,2)],{type:"application/json"}));a.download="mike-ajijola-cv.json";a.click()}};return <div className="shell"><div className="page-head cv-head"><div><div className="eyebrow">Curriculum vitae</div><h1 className="page-title">Mike Ajijola</h1><p>Platform engineer and technical leader.<br/>London, United Kingdom.</p></div><div className="actions"><button className="secondary" onClick={exportJson}>Export JSON</button><button className="primary" onClick={()=>execute("cv.print")}>Print CV</button></div></div><div className="cv-sheet"><section className="cv-section"><h3>Profile</h3><p className="lead" style={{margin:0}}>I build the foundations that help engineering teams move with confidence: capable platforms, observable systems and clear technical direction.</p></section><section className="cv-section"><h3>Experience</h3><div>{experience.map(x=><div key={x.id} style={{marginBottom:30}}><b>{x.title} · {x.organisation}</b><div className="muted">{x.period}</div><p>{x.summary}</p></div>)}</div></section><section className="cv-section"><h3>Selected work</h3><div>{projects.slice(0,3).map(p=><p key={p.id}><b>{p.name}</b><br/><span className="muted">{p.summary}</span></p>)}</div></section><section className="cv-section"><h3>Capabilities</h3><div className="tags">{skills.map(s=><span className="tag" key={s.id}>{s.name}</span>)}</div></section></div></div>}
